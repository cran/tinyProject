#!/usr/bin/env sh

IMAGE_VERSION=$1
echo "#################################################"
echo  "Building image LTV_AUTOMATED version : $IMAGE_VERSION"
echo "#################################################"

# Build base image
docker build -t <= options("projectRoot") => -f ./docker/Dockerfile .
echo "Build base image finished"

exit 0